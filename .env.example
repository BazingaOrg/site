# GitHub publishing
GITHUB_TOKEN=your_github_token
GITHUB_REPO=owner/repo
WRITE_ACCESS_KEY=your_write_access_key

# Cloudflare R2 → photos.json sync (npm run photos:sync-from-r2)
# Create an API token with Account → Workers R2 Storage → Read
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_API_TOKEN=your_api_token
R2_BUCKET=bazinga-gallery
PHOTOS_CDN=https://img.bazinga.ink
PHOTOS_PREFIX=photos/

# Preferred (afilmory-style): npm run photos:build-variants
# Writes WebP under images/photos/variants/ (no R2 write). Wired into Vercel buildCommand.
#
# Vercel / CI: public CDN often returns 403 to datacenter IPs. Set these so the build
# downloads via R2 API first (Read is enough):
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=   # Account → Workers R2 Storage → Read
# R2_BUCKET=bazinga-gallery
# PHOTOS_CDN=https://img.bazinga.ink
# PHOTOS_DOWNLOAD_MODE=api   # optional force; auto on Vercel when token is set
#
# Optional knobs:
# PHOTOS_BUILD_CONCURRENCY=2
# PHOTOS_THUMB_WIDTH=720
# PHOTOS_THUMB_QUALITY=78
